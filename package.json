{
  "name": "pouchzot",
  "version": "0.1.0",
  "license": "AGPL-3.0-or-later",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "check": "npm run typecheck && npm test -- --run",
    "build": "npm run check && vite build",
    "build:full": "engine/crawl-ref/source/wasm/release.sh && npm run build && cp -a engine/crawl-ref/source/wasm/dist/site/. dist/",
    "release:engine": "engine/crawl-ref/source/wasm/release.sh --publish && node .github/fetch-offline.mjs --update-pin engine/crawl-ref/source/wasm/dist/release && git commit -m \"Update offline engine release\" -- .github/offline-engine.json && git push origin main",
    "preview": "vite preview"
  },
  "devDependencies": {
    "happy-dom": "^20.11.2",
    "typescript": "^7.0.2",
    "vite": "^7.0.0",
    "vitest": "^4.1.7"
  }
}
